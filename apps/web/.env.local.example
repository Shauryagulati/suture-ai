# Copy to .env.local (do NOT commit)

# NextAuth v5 (Auth.js) uses AUTH_SECRET. Generate with:
#   openssl rand -base64 32
AUTH_SECRET=
AUTH_URL=http://localhost:3000

# Pointer to the FastAPI backend.
API_URL=http://localhost:8000
